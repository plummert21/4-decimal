#include "s21_compare.h"
#include "s21_decimal.h"
#include "s21_func_bits.h"

int s21_is_equal(s21_decimal value_1, s21_decimal value_2) {
  int result = -1;
  // Copy decimal to long decimal
  // Normalize
  // Compare long decimal
  // if result compare = 7 -> equal.
  return result;
}
